const Button = document.querySelector(".dropbtn")
const Content = document.querySelector(".dropdown-content")

const Drop = () => {
    console.log(Button)
    console.log(Content)
    // Button.style.backgroundColor = "#dd995e"
    // Content.style.display = "block"
}

export default Drop